spring:
  application:
    name: user-service  # Имя должно совпадать с именем файла в config-repo!
  cloud:
    config:
      uri: http://localhost:8888  # Адрес Config Server
      fail-fast: false  # Если config-server недоступен — продолжаем работу с локальным конфигом